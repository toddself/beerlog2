Spine.Model.extend({
    setupFromList: function(name, list){
        return Spine.Model.setup(name, list);
    }
});

$.ready(function(){
    var models = ['Hop', 'Grain', 'Extract', 'HoppedExtract', 'Fining',]
})
